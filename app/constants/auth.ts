export const STORAGE_KEYS = {
  token: 'token',
} as const;

export const AUTH_ERROR_MESSAGES = {
  loginFailed: 'Login failed',
  registrationFailed: 'Registration failed',
  currentUserFailed: 'Failed to fetch user',
} as const;
